# screen saver script

custom `phosphor` clock in xscreensaver

### install
copy and paste into a terminal:

```bash
bash -c "$(curl -fsSL https://raw.githubusercontent.com/austinp0573/screen-saver-script/main/install-rolling-clock.sh)"
```